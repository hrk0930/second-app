<div class="chat-header3">
<div class="room1">

    <select name="select" onChange="location.href=value;">
          <option>チャットするルームを選択してください</option>
          <option value="messages#index">ニコニコルーム１</option>
          <option value="chats#index">グリーンルーム</option>
          <option value="memos#index">メモ</option>
        </select>
  </div>
</div>

<div class="messages0">
<%= render partial: 'memo', collection: @memos, class: 'messages1'%>
</div>

<%= form_with model: @memo, class: 'form-2', local: true do |f| %>
  <div class="form-input1">
    <%= f.text_field :text, class: 'form-message1', placeholder: 'メモを書いてね〜♫' %>
  </div>
  <%= f.submit '送信', class: 'form-submit1' %>
<% end %>